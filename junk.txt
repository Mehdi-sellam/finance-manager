INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',

    # third-party apps
    'rest_framework',

    # your apps
    'accounts',
    'users',
    'projects',
    'finance',
]


from django.db import models
from accounts.models import BusinessOwner

# Create your models here.


class Employee(models.Model):
    user = models.OneToOneField(User, on_delete=models.CASCADE)
    owner = models.ForeignKey(BusinessOwner, on_delete=models.CASCADE)
    name = models.CharField(max_length=255)
    role = models.CharField(max_length=255)
    monthly_salary = models.DecimalField(max_digits=10, decimal_places=2)

    def __str__(self):
        return f"{self.user.username} ({self.owner.company_name})"

{
  "amount": 3000,
  "date": "2022-03-03",
  "employee": 2,
  "project": 1
}


🎉 That's it — Steps 12 & 13 are fully implemented!

You now have:

✔ Nested API endpoints
✔ Auto-linked project references
✔ Filtering, search, ordering
✔ Pagination
✔ Budget validation logic
✔ Clean URL architecture

Everything is ready for authentication, Swagger beautification, and final deployment.

🚀 Want me to continue to Step 10 (Authentication) next?

I can set up:

🔐 JWT Authentication (SimpleJWT)
🔐 Automatically protect endpoints
🔐 Add login/logout endpoints
🔐 Swagger authorization button
🔐 Permission logic (owners, employees, admin)

Just say “Yes, continue to Step 10”.



{
    "projects": "http://127.0.0.1:8000/api/projects/",
    "users": "http://127.0.0.1:8000/api/users/",
    "owners": "http://127.0.0.1:8000/api/auth/owners/",
    "expenses": "http://127.0.0.1:8000/api/expenses/",
    "salaries": "http://127.0.0.1:8000/api/salaries/",
    "resources": "http://127.0.0.1:8000/api/resources/",
    "budgets": "http://127.0.0.1:8000/api/budgets/"
}



superuser access API Token:

{
    token 82a9a501db110bb071dc302a530d27d97c047b78
    "username": "sellam"
}

